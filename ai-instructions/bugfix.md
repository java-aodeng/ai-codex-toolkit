项目路径：D:\work\cloud-skyte

浏览器中已经打开阿里文档 Bug 清单和本地测试页面。请按照以下规则持续处理，不需要每条都等我确认。

一、处理范围

1. 只处理阿里文档当前筛选后显示的 Bug。
2. 当前筛选条件：
    - 优先级：【填写，例如 P3】
    - 问题状态：【填写，例如 未开始】
3. 从当前第一条未处理记录开始，严格按照表格从上到下的顺序处理。
4. 每条都要读取：
    - 行号
    - 模块
    - 问题描述
    - 附件截图
    - 备注和未通过描述（如果有）
5. 不要导出或下载表格，直接在浏览器中读取和回填。
6. 表格中没有新的筛选命中记录后才结束。

二、浏览器规则

1. 单独创建并固定使用两个自己的 Chrome 标签页：
    - Bug 表格标签页
    - 本地系统验证标签页
2. 不要使用我正在操作的标签页，避免互相影响。
3. 我操作、滚动或切换自己的页面时，不应影响你的两个标签页。
4. 如果 Chrome 提示未连接到可控页面，主动重新连接。
5. 如果表格页面被我滚动或切换，导致无法确认单元格位置，立即暂停回填，不要猜测行号；代码工作可以继续，等位置明确后再填。
6. 不要因为表格操作卡住而长时间停留，问题修复是主要任务。

三、代码处理规则

1. 开始前读取：
    - D:\work\ai-dev-rules\rules\coding-standards.mdc
    - 如果修改 skyte-ui-new，再读取：
      D:\work\cloud-skyte\skyte-ui-new\AGENTS.md
2. 不使用子代理，由当前会话直接处理。
3. 搜索优先使用 rg -n，只读取与当前问题直接相关的代码和引用链路。
4. 保留工作区已有未提交改动，不覆盖、不撤销，也不要把无关改动带入提交。
5. 每条 Bug 先复现和定位根因，再做最小范围修改。
6. 不做无关重构、全局格式化或顺手优化。
7. 每修复一条实际存在的问题，单独创建一个 Git 提交。
8. 一个提交只能包含当前这一条 Bug 的改动。
9. 不主动推送远程仓库，除非我明确要求。
10. 如果当前代码已经修复：
    - 在实际页面验证
    - 不制造空提交
    - 直接回填表格并继续下一条
11. 如果问题无法复现，要检查实际 DOM、计算样式、接口请求或控制台，不能只凭代码判断。

四、验证规则

前端可以独立验证的问题：

1. 在本地系统验证标签页进入对应真实页面。
2. 使用真实数据和真实操作路径复现。
3. 检查：
    - 页面视觉效果
    - DOM 和计算样式
    - 点击、悬浮、弹窗、分页等交互
    - 浏览器控制台错误
    - 文本是否溢出、遮挡、重叠
4. 运行与改动相关的轻量检查，例如：
    - npm run build
    - git diff --check
5. 验证通过后提交。
6. 表格“ai校验”填写“是”。

需要重启后端、执行数据库迁移或依赖后端新接口的问题：

1. 代码仍然正常完成，包括必要的前端、后端和 SQL 修改。
2. 做静态检查和前端可验证部分。
3. 不主动启动或重启后端。
4. 不因为等待后端而卡住，完成提交后继续下一条。
5. 表格“ai校验”填写“否”。
6. 最终总结中说明需要重启的服务、SQL 脚本或未完成的运行时验证。

五、表格回填规则

每条完成并提交后，回填当前 Bug 所在行：

- 处理人：【填写指定处理人】
- 处理时间：【填写指定日期，例如 2026/8/5】
- ai校验：
    - 前端已自行完整验证：是
    - 需要重启后端、执行 SQL 或无法完成后端运行时验证：否

注意：

1. 不修改“问题状态”。
2. 不修改“待验收”“验收完成”等状态。
3. 状态由我或产品人员处理。
4. 不填写与本次处理无关的列。
5. 回填后重新确认行号和内容，避免填错行。

六、执行节奏

每条按照以下顺序执行：

读取表格和附件
→ 复现问题
→ 定位根因
→ 修改代码
→ 自行验证
→ 单独提交
→ 回填处理人、处理时间、ai校验
→ 立即处理下一条

不需要每条等待我回复“确认通过”。除非：

- 需求描述与附件互相矛盾
- 无法确定正确业务规则
- 表格位置被移动，可能填错行
- 修改会明显扩大需求范围
- 涉及破坏性操作或无法恢复的数据变更

七、过程沟通

1. 每处理一条，简要告诉我：
    - 当前行号和问题
    - 定位到的原因
    - 修改内容
    - 验证结果
    - 提交 ID
    - ai校验填写“是”还是“否”
2. 不要长时间无消息。
3. 如果某一步校验耗时过长，说明卡点并先处理下一条。
4. 全部完成后汇总：
    - 已处理行号
    - 每条提交 ID
    - 已验证项目
    - ai校验为“否”的项目及原因
    - 未处理或需要人工确认的项目

现在开始处理，不需要先给方案，也不要停在分析阶段。